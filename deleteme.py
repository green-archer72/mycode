!#/bin/rm
# This script calls on the executable calld **rm** (remove), which is
# installed to the */bin* directory. When this script is run,
# bash will ececute this script with *rm*, which will remove (delete)
# the script :p
